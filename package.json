{
  "name": "ai-todo-app",
  "version": "1.0.0",
  "description": "An AI-powered todo application with natural language processing and smart prioritization",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server-new && npm run start",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["todo", "ai", "nlp", "productivity"],
  "author": "AI Todo Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}